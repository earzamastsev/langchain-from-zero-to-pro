# 🤖 E-commerce Chat Bot (Task Module №2)

Интеллектуальный чат-бот для сервиса доставки и заказов, построенный на базе LangChain и OpenAI. Бот помогает пользователям получать информацию о заказах, доставке и отвечает на часто задаваемые вопросы.

## ✨ Возможности

- 💬 **Интерактивный диалог** с пользователями на русском языке
- 📦 **Отслеживание заказов** через команду `/order <номер_заказа>`
- ❓ **Ответы на FAQ** на основе базы знаний
- 🧠 **Контекстная память** для ведения осмысленных диалогов
- 📝 **Логирование** всех взаимодействий
- 💾 **Сохранение сессий** в формате JSONL с метриками использования
- 📊 **Отслеживание токенов** для мониторинга расходов API
- ⚡ **Быстрые ответы** с обработкой ошибок
- 🎯 **Структурированные ответы** с контролем тона и рекомендуемыми действиями
- 📋 **Система оценки стиля** для контроля качества ответов бота
- 🔧 **Гибкая конфигурация** промптов и стиля через YAML файлы
- 📈 **Автоматическая оценка** соответствия ответов бренд-стилю

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.13+
- [uv](https://docs.astral.sh/uv/) - современный менеджер пакетов Python
- API ключ OpenAI

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone git@github.com:earzamastsev/langchain-from-zero-to-pro.git
   cd Module-2
   ```

2. **Установите зависимости:**
   ```bash
   uv sync
   ```

3. **Настройте переменные окружения:**
   Создайте файл `.env` в корневой директории:
   ```env
   OPENAI_API_KEY=your_openai_api_key_here
   OPENAI_API_BASE=https://api.openai.com/v1
   OPENAI_MODEL=gpt-4о-mini
   ```

### Запуск

```bash
uv run python app.py
```

## 📁 Структура проекта

```
module-2/
├── app.py                    # Основное приложение
├── style_eval.py            # Скрипт оценки стиля ответов
├── data/
│   ├── faq.json             # База часто задаваемых вопросов
│   ├── orders.json          # База данных заказов
│   ├── prompts.yaml         # Конфигурация промптов
│   ├── style_guide.yaml     # Руководство по стилю бренда
│   ├── few_shots.jsonl      # Примеры диалогов для обучения
│   └── eval_prompts.txt     # Промпты для тестирования
├── src/
│   ├── brand_chain.py       # Основная логика чат-бота
│   ├── schema.py            # Pydantic схемы для ответов
│   └── style_eval.py        # Модуль оценки стиля
├── logs/
│   ├── chat_session.log     # Логи диалогов
│   └── session_*.jsonl      # Файлы сессий с метриками
├── reports/
│   └── style_eval.json      # Отчеты оценки стиля
├── pyproject.toml           # Конфигурация проекта
├── uv.lock                  # Заблокированные версии зависимостей
└── README.md                # Документация
```

## 🎯 Использование

### Основные команды

- **`/order <номер_заказа>`** - получить статус заказа
- **`выход`** / **`quit`** / **`exit`** - завершить работу бота

### Примеры диалогов

```
Вы: /order 12345
Бот: Заказ 12345: в пути. Ожидаемая доставка: 2 дней. Служба доставки: ShoplyExpress

Вы: Как отследить заказ?
Бот: Для отслеживания заказа используйте команду /order с номером вашего заказа...

Вы: выход
Бот: До свидания!
```

### Структурированные ответы

Бот возвращает структурированные ответы в формате JSON с тремя полями:

```json
{
  "answer": "Краткий ответ на вопрос пользователя",
  "tone": "Контроль тона: совпадает ли тон (да/нет) + одна фраза почему",
  "actions": ["Список следующих шагов для клиента (0-3 пункта)"]
}
```

**Пример структурированного ответа:**
```json
{
  "answer": "Стандартная доставка 2–5 рабочих дней. Экспресс — в течение 24–48 часов.",
  "tone": "да, вежливый и деловой тон без эмодзи и восклицаний",
  "actions": [
    "Выберите способ доставки при оформлении заказа",
    "Проверьте статус заказа через команду /order <номер_заказа>"
  ]
}
```

## ⚙️ Конфигурация

### Настройка данных

#### FAQ (data/faq.json) 

Файл с часто задаваемыми вопросами и ответами. Например
```json
  [
  {"q": "Как оформить возврат?", "a": "Вы можете оформить возврат в течение 14 дней. Сохраните чек и упаковку."},
  {"q": "Сколько идёт доставка?", "a": "Стандартная доставка 2–5 рабочих дней. Экспресс — в течение 24–48 часов."},
  {"q": "Какие способы оплаты?", "a": "Карты Visa/Mastercard, СБП, подарочные сертификаты Shoply."},
  {"q": "Как изменить адрес после заказа?", "a": "Напишите нам в чат как можно скорее — если заказ не отгружен, мы поменяем адрес."},
  {"q": "Как применить промокод?", "a": "Введите промокод в корзине перед оплатой. Один промокод на заказ."}
]
```

#### Заказы (data/orders.json)

Файл с информацией о заказах. Например:
```json
{
  "12345": {
    "status": "in_transit",
    "eta_days": 2,
    "carrier": "ShoplyExpress"
  },
  "98765": {
    "status": "delivered",
    "delivered_at": "2025-08-10"
  }
}
```

#### Промпты (data/prompts.yaml)

Конфигурация системных и пользовательских промптов:
```yaml
prompts:
  current: v1
  v1:
    system: |
      Ты — полезный и лаконичный ассистент сервиса доставки и заказа "{brand}".
      # ... системный промпт
    user: |
      Подготовь ответ на вопрос или сообщение пользователя: "{input}"
      # ... пользовательский промпт
```

#### Стиль бренда (data/style_guide.yaml)

Руководство по стилю общения бота:
```yaml
brand: Shoply
tone:
  persona: "вежливый, деловой, дружелюбный без панибратства"
  sentences_max: 3
  bullets: true
  avoid:
    - "эмодзи"
    - "восклицания!!!"
    - "канцелярит: согласно вышеизложенному, осуществите"
  must_include:
    - "простые формулировки"
    - "варианты решения, если есть"
fallback:
  no_data: "У меня нет точной информации. Могу подключить оператора или оформить запрос."
format:
  fields:
    answer: "краткий ответ"
    tone: "контроль: совпадает ли тон (да/нет) + одна фраза почему"
    actions: "список следующих шагов для клиента (0–3 пункта)"
```

## 💾 Сохранение сессий и метрики

### Файлы сессий

Каждая сессия диалога сохраняется в отдельный файл `logs/session_<uuid>.jsonl` в формате JSONL (JSON Lines). Каждая строка содержит:

```json
{
  "dialog": "User: вопрос пользователя\nBot: ответ бота",
  "usage": 470
}
```

### Структура файла сессии

```jsonl
{"dialog": "User: привет\nBot: Привет! Как я могу помочь?", "usage": 470}
{"dialog": "User: статус заказа 12345\nBot: Заказ 12345: в пути", "usage": 520}
{"dialog": "User: спасибо\nBot: Пожалуйста! Обращайтесь еще!", "usage": 380}
```

## 📊 Система оценки стиля и качества

### Автоматическая оценка ответов

Проект включает систему автоматической оценки качества ответов бота по двум критериям:

1. **Правила (rule_checks)** - проверка технических требований:
   - Отсутствие эмодзи (-20 баллов)
   - Отсутствие множественных восклицательных знаков (-10 баллов)
   - Длина ответа не более 600 символов (-10 баллов)

2. **LLM-оценка** - оценка соответствия бренд-стилю с помощью GPT-4o-mini

### Запуск оценки

```bash
# Оценка стиля ответов на тестовых промптах
uv run python style_eval.py
```

### Структура отчета

Отчет сохраняется в `reports/style_eval.json`:

```json
{
  "mean_final": 90.1,
  "items": [
    {
      "prompt": "Сколько идёт доставка?",
      "answer": "Сроки доставки зависят от вашего региона...",
      "actions": ["Уточните свой адрес...", "Проверьте статус заказа..."],
      "tone_model": "да, вежливый и деловой тон без эмодзи и восклицаний",
      "rule_score": 100,
      "llm_score": 85,
      "final": 91,
      "notes": "Соответствует тону бренда: вежливый, деловой..."
    }
  ]
}
```

### Тестовые промпты

Промпты для тестирования хранятся в `data/eval_prompts.txt`:
```
Сколько идёт доставка?
Можно ускорить доставку?
Где ввести промокод?
У меня сломан товар, что делать?
...
```